# 📌 PROJECT MEMORY — Jikoni POS / Duka Kiganjani (donel_pos)

> **Kwa Buffy (AI agent):** Soma file hii MWANZO wa kila session kabla ya kufanya lolote.
> Mwisho wa kila kazi, UPDATE sehemu za "Hali ya sasa", "Kazi inayoendelea" na "Log".
> Mtiririko: `# Summary ya mwisho` → `# Mpango wa phases` → `# Hali ya sasa` → `# Kazi inayoendelea` → `# Maamuzi` → `# Log`.

---

# Summary ya mwisho

Project: **Jikoni POS / Duka Kiganjani** — Flutter app (POS/duka) + PHP backend (focustec.co.tz kupitia ftp.dadcas.com, `/pos/api/`).
Lugha ya mazungumzo: **Kiswahili**. UI ina SW/EN (l10n).
Ukaguzi kamili (audit) ulifanyika 2026-09-14: features 140 zilichunguliwa — app (Flutter, ~33k mistari), backend (PHP 8, endpoints 49), DB (MySQL, tables 22). Mpango wa utekelezaji ni phases 1–7 (tazama chini).

# Mpango wa phases (kutoka audit)

- **Hatua 1 (P0, ~siku 3) — Usalama wa msingi**: DB/FTP password mpya, backup ya usiku, api_tokens + Bearer, bootstrap.php (auth/permissions/audit), require_permission kwenye writes, business_id kutoka token, app ifiche vitufe kwa role.
- **Hatua 2 (P0, ~siku 4) — Wateja + malipo + marejesho**: customers table + backfill + picker POS + statement; sale_payments (split: Cash/M-Pesa/Bank) + change + historia; returns/refund/exchange + credit note; Checkout widget moja (desktop+simu).
- **Hatua 3 (P0, ~siku 3) — Stock ledger**: stock_movements + opening backfill + StockService; adjustments (damaged/lost/count/opening) + approval; stock card; unganisha batches mbili kuwa moja; FEFO.
- **Hatua 4 (P1)**: register/shift (fungua/funga, expected vs actual), VAT settings + risiti, discount ya jumla + approval ya manager (PIN), hold/resume, risiti (logo, cashier, QR, 58/80mm, A4).
- **Hatua 5 (P1)**: suppliers + PO→GRN→invoice→payment→statement, stock transfer kati ya matawi, accounts (Cash/M-Pesa/Tigo/Airtel/Bank) + cash flow + reconciliation.
- **Hatua 6 (P2)**: documents (quotation/proforma/delivery note), receipt template designer (JSON), journal ya automatic + trial balance + balance sheet, ripoti filters + PDF/CSV, slow/dead stock, design system ya Flutter.
- **Hatua 7 (P3)**: online shop + orders + stock reservation + M-Pesa STK, AI assistant, TRA/EFD (kupitia VFD aliyeidhinishwa tu).

# Hali ya sasa (kama ya 2026-09-15)

## Hatua 1 — Usalama: ✅ IMEKAMILIKA na IME-DEPLOY SERVER
- `helpers/bootstrap.php` (auto-include kupitia db.php): api_tokens (SHA-256 hash, 30d, sliding renewal), audit_log, login_throttle (5 fails → lock 15min), require_permission(), auth_check_business_scope(), roles/permissions (Owner/Admin=*, Manager, Accountant, Stockist, Cashier, Supporter, Viewer).
- login.php inatoa `auth_token`; app (api_service.dart) inahifadhi na kutuma `Authorization: Bearer` kila ombi ✅.
- Legacy window: app za zamani bila token zinaruhusiwa hadi **2026-10-31** (`AUTH_LEGACY_UNTIL`); baada ya hapo token PEKEE. Muda huo: source=legacy inapita permission checks (tabia ya zamani).
- Endpoints 19/45 zina `require_permission()` waziwazi (writes muhimu); legacy bypass inafunika tofauti mpaka window ifunge.
- ✅ **Backup ya usiku (2026-09-16)**: `db_backup.php` (pure-PHP dump ya PDO — haitegemei mysqldump/shell_exec, kwa vile shared hosting nyingi zinaizima) ime-deploy kwenye `/pos/api/db_backup.php`. Inatoa `.sql.gz` (jedwali zote + rows) ndani ya `/pos/backups_private/` (nje ya `api/`, imelindwa na `.htaccess` `Require all denied` — imejaribiwa live: directory listing na direct-file-guess zote 403), jina la faili lina sehemu ya nasibu isiyokisiwa. Rotation: siku 14 za mwisho pekee zinabaki. Kuendesha: CLI (cron ya server, hauhitaji key) au HTTP `?key=<SECRET>` (kwa huduma za nje kama cron-job.org). **SECRET haijaandikwa humu kwa makusudi** — ipo kwenye `server_mirror/api/backup_key.php` (faili la ndani, halijawahi kuwa kwenye git, `server_mirror/` iko `.gitignore`) — fungua faili hilo mwenyewe ukihitaji thamani yake kuweka kwenye cron. Imejaribiwa live 2026-09-16: jedwali 53, safu 2314, ~47 KB — mafanikio.
- ❌ **Bado kunahitajika hatua ya mtumiaji (nje ya uwezo wangu — sina cPanel/SSH access)**: (a) ongeza Cron Job kwenye cPanel ya focustec.co.tz — mf. schedule `0 2 * * *` (saa 8 usiku kila siku), command: `wget -q -O /dev/null "https://focustec.co.tz/pos/api/db_backup.php?key=SECRET_KENYE_HAPO_JUU"` (au tumia huduma ya nje kama cron-job.org kwa URL hiyo hiyo); (b) offsite copy — backups_private ipo kwenye server hilo hilo (single point of failure) — pendekezo: weka rclone/cPanel "Backup to remote" au download ya kila wiki kuelekea Google Drive/S3.
- **HAIJAFANYIKA bado**: kubadilisha DB/FTP passwords (zilizoonekana kwenye chat awali — bado ni hatari), HSTS, uploads hardening.

## Hatua 2 — Wateja/Malipo/Marejesho: ✅ IMEKAMILIKA (kwa vitendo)
- ✅ `customers` table + `customers.php` (list/get/add/update/delete; get = profile + statement: sales + tbl_loan_payments + summary).
- ✅ App: `lib/services/crm_api.dart` (listCustomers/getCustomer/saveCustomer/deleteCustomer + offline cache/push) + `customers_screen.dart` (list, debtors, VIP, statement).
- ✅ POS: kitufe "Chagua mteja" (`_PickCustomerButton`) — inafungua CustomersScreen(pickMode) na kujaza jina/simu kwenye checkout form.
- ✅ Historia ya malipo: `get_sale_payments.php` (`payments` = tbl_loan_payments, auto-andikwa na trigger `trg_sales_ai`/`trg_sales_au` kila `paid_amount` inapoongezeka) + app `getSalePayments()`. **⚠️ `pay_loan.php` ni dead code** — inaandika kwenye columns `business_id/payment_method/notes/created_at/status/due_amount` ambazo HAZIPO kwenye tbl_loan_payments/tbl_sales za live (columns halisi: `payment_date/note/source/received_by` na `paid_amount/balance_amount/payment_status`) — ingefeli ikiitwa. App haiiti popote (`grep` haina matokeo) — labda ilibadilishwa na `sale_action.php`'s `record_payment` bila kuifuta. Haijaguswa (nje ya scope), lakini ni hatari ikiwa mtu ataitumia baadaye bila kujua.
- ✅ `customer_id` sasa inahifadhiwa kwenye tbl_sales moja kwa moja (`create_sale.php` inathibitisha id dhidi ya business_id kabla ya kuamini); POS (`pos_screen.dart`) inatuma `customerId`/`customerPhone` badala ya string tu, offline queue (`offline_api_service.dart`/`sync_service.dart`) inatatua temp-id→real-id kabla ya sync.
- ✅ **Returns/refunds**: `sale_returns.php` (list/create) — inarudisha stock (trigger iliyopo), inahesabu upya total/paid/balance/payment_status ya mauzo, inaonya cash ya kurudisha mteja pale inapohitajika; `sale_return_sheet.dart` UI (item picker + stepper), kitufe "Rudisha Bidhaa" kwenye `transaction_detail_sheet.dart` kikiwa kimefungwa na `user.canReturnSales` (cashier+). Online-only kwa makusudi (haijawekwa kwenye `OfflineApiService`). Imekwisha-deploy + push kwenye PR #1 (commit 3f9861a, 2026-09-16).
- ✅ **Split payment (2026-09-16)**: `sale_payments` table (self-creating, kama `sale_returns`) — mauzo ya `cash` (au deposit ya `slow_payment`) yanaweza kulipwa kwa njia zaidi ya moja (Cash+M-Pesa+Bank) kwa wakati mmoja, na kupata chenji kwa cash iliyozidi. UI: `lib/widgets/split_payment_field.dart` — toggle "Gawanya malipo" (imezimwa kwa default, haiathiri njia ya kawaida ya malipo moja). Haibadilishi trigger za `tbl_loan_payments` zilizopo — `sale_payments` ni breakdown ya ziada tu, iliyounganishwa kwenye `get_sale_payments.php`'s `breakdown` key na kuonyeshwa kama chips kwenye `transaction_detail_sheet.dart`. Inafanya kazi offline pia (payload inapita kwenye sync queue). `flutter analyze` 0, `flutter test` 27/27, deployed + push kwenye PR #1 (commit fa58a91).
- ✅ **Backfill ya wateja imekamilika** (imethibitishwa live 2026-09-16 kwa diagnostic query): mauzo 40 jumla, 5 zina customer_id, 0 zina phone-lakini-hazina customer_id (yaani backfill ya awali ya migration_ledger_customers.sql + trigger `trg_sales_bi` tayari zimeshamaliza kazi), 35 hazina phone kabisa (walk-in customers wasioweza kuunganishwa — hakuna cha kufanya kwao). Customers table ina rekodi 3. **Hakuna kazi zaidi inayohitajika hapa.**
- ✅ **Credit notes (2026-09-16)**: baada ya return kufanikiwa, `_CreditNoteSheet` (ndani ya `sale_return_sheet.dart`) inaonyesha na kuchapisha "Hati ya Marejesho" (namba `CN-YYYYMM-NNNNN` kutoka `return_id`, bidhaa zilizorudishwa, imepunguza deni kiasi gani, kurudishiwa taslimu kiasi gani) — inatumia pw.Document/Printing pattern ileile ya risiti ya mauzo. Hakuna DB/backend mabadiliko yaliyohitajika (namba inatengenezwa client-side).
- ❌ **Bado hakuna**: split payment kwa `slow_payment` haijajaribiwa live (logic ipo lakini haijapata real-world test), Checkout widget moja (desktop `_CartPanel` + simu `_CartSheet` bado mbili — uamuzi wa makusudi kuahirisha).

## Hatua 3 — Stock ledger: ✅ IMEKAMILIKA (kwa vitendo)
- ✅ `stock_movements.php` (stock card + recent movements) + `adjustStock` kwenye crm_api (adjustment sheet kwenye app).
- ✅ **Batch tables + FEFO (2026-09-16)**: iligundulika `product_batches` (FIFO/remaining, ndiyo halisi inayotumika na create_sale.php + get_product_batches.php) na `tbl_product_stock_batch` (log tu, HAISOMWI na chochote) zilikuwa mbili tofauti — stock ya bidhaa mpya (`add_product.php`) na bulk import (`import_products.php`) ziliandikwa tbl_product_stock_batch PEKEE, hazikuonekana kwenye FIFO. Sasa `helpers/batches.php`'s `product_batch_add()` inaandika `product_batches` pia (additive, haikuondoa maandishi ya zamani). `create_sale.php` na `get_product_batches.php` sasa zina **FEFO** order (`expiry_date IS NULL ASC, expiry_date ASC, date_added ASC`) badala ya FIFO tu. `add_stock.php` ni dead code (app haiiti — grep haina matokeo), haikuguswa. Deployed + smoke-tested live, push (commit 2a9007b).
- ✅ **Manager PIN approval (2026-09-16)**: `helpers/manager_pin.php` (`pin_hash` column kwenye tbl_user, self-adding) + `manager_pin.php` endpoint (set/verify). `stock_movements.php`'s adjust action sasa inahitaji PIN sahihi kwa write-offs za hasara (damaged/lost/correction-minus) ISIPOKUWA mtumaji ni Owner/Admin/Manager/SuperAdmin tayari — meneja YEYOTE aliyepo anaweza kuidhinisha. UI: `manager_pin_dialog.dart` (numeric keypad dialog), kitufe "Weka PIN ya Idhini" kwenye Profile (manager-tier pekee), kimeunganishwa kwenye `StockAdjustSheet`. Jina la aliyeidhinisha linaongezwa kwenye ledger reason. Reusable kwa Hatua 4's "discount + manager PIN". Deployed + smoke-tested live, push (commit a447ab0). **Hii ndiyo kipengele cha mwisho cha P0 kwenye Hatua 3 — Hatua 3 sasa IMEKAMILIKA kwa vitendo.**
- ❌ Bado (si P0, si dharura): StockService moja kwa writes ZOTE (usanifu tu — ledger tayari ipo kwa writes zote kupitia trigger `trg_product_au`), opening-balance backfill kwa bidhaa za zamani (hali haijulikani — angalia `stock_movements` movement_type='opening' counts).

## Hatua 4 — P1: ✅ IMEKAMILIKA (siyo P0, lakini kazi imeanza kabla ya wakati)
- ✅ **Punguzo la jumla + idhini ya meneja (2026-09-16)**: `DiscountField` widget kwenye POS checkout (imezimwa kwa default), `create_sale.php` inakubali `overall_discount`; punguzo linalozidi 10% ya subtotal linahitaji `manager_pin` sahihi (kutumia `helpers/manager_pin.php` ile ile ya Hatua 3) isipokuwa mtumaji ni Owner/Admin/Manager/SuperAdmin. Grand total (baada ya punguzo) inatumika kwa split payment target + risiti (Total/Paid/Change) — si `cart.total` ya subtotal. Inafanya kazi offline (discount+PIN zinapitia sync queue). Deployed + smoke-tested live, push (commit 65d1a93).
- ✅ **Hold/Resume Sale (2026-09-16)**: `HeldSalesProvider` (local-only, kv_cache — si sale halisi mpaka i-checkout, hivyo hakuna sababu ya kuisync na server). Kitufe "Weka Kando" (Hold) na "Mauzo Yaliyowekwa Kando" (badge yenye idadi) kwenye POS checkout (desktop + simu). `CartItem` sasa ina `toJson()/fromJson()`. Held sales zinabaki baada ya app kufungwa, zinafutwa wakati wa logout (pamoja na kv_cache nyingine). `flutter analyze` 0, `flutter test` 27/27 (ilibidi kurekebisha layout overflow ndogo kwenye desktop panel + kuongeza HeldSalesProvider kwenye screenshot test's provider tree). Hakuna backend/DB. Push (commit 3065d75).
- ✅ **Risiti ya kitaalamu zaidi (2026-09-16)**: logo ya biashara (kama ipo, `business.logoPath`), jina la muuzaji (cashier), QR code (encoding: biashara+namba ya risiti+tarehe+jumla — kwa uthibitisho wa haraka), na chaguo la ukubwa wa karatasi (58mm/80mm/A4) linalokumbukwa per-device kupitia `StorageService`. `_PosReceiptSheet` sasa ni Stateful. Logo inapakuliwa mtandaoni wakati wa kuchapisha, inarukwa kimya ikishindikana (haiathiri kuchapisha). Package mpya `barcode` (ilikuwa transitive tayari via pdf/printing) — jina lake `Barcode` liligongana na `mobile_scanner`'s Barcode, limetatuliwa kwa import alias (`as bc`). Hakuna backend/DB. `flutter analyze` 0, `flutter test` 27/27, push (commit 344803e).
- ✅ **Register/Shift + reconciliation (2026-09-17)**: `shifts.php` mpya — cashier anafungua zamu na fedha ya kuanzia (opening_cash), anauza kama kawaida, kisha anafunga akihesabu fedha taslimu mkononi. Kufunga kunahesabu "fedha inayotarajiwa" = opening_cash + cash sale_payments - cash tbl_expenses za wakati wa zamu, na kuonyesha tofauti (variance) — **hii NDIYO reconciliation ya Hatua 5**, hakuna feature tofauti iliyohitajika. `ShiftProvider` + kitufe cha register (chenye dot ya kijani ikiwa zamu iko wazi) kando ya kitufe cha Held Sales kwenye POS (desktop+simu zote mbili) kinafungua `ShiftSheet` (fungua/funga/historia). **Niligundua na kurekebisha bug halisi wakati wa smoke-test live**: `$ctx['user_id'] ?? intv($data['user_id'] ?? 0)` haifanyi kazi wakati auth_context() inarudisha user_id=0 (si null) — jambo la kawaida wakati wa dirisha la legacy (bila token) — `??` haiangalii 0 kama "haipo", hivyo user_id ya mtumiaji aliyeko kwenye app ilikuwa ikipuuzwa kimya na kila jaribio la kufungua zamu lilishindwa. Nilijaribu mzunguko kamili (fungua→funga→variance) dhidi ya live kwa mtumiaji wa jaribio (user_id ya kubuni), nikathibitisha hesabu ni sahihi (50,000 + 0 - 0 = 50,000 inayotarajiwa; kaunta 52,000 = tofauti +2,000), kisha nikafuta data hiyo ya jaribio. `flutter analyze` 0, `flutter test` 27/27 (nilijaribu screenshot ya POS kwa macho kuhakikisha kitufe kipya hakiathiri mpangilio), push (commit 99da20f).
- ❌ **VAT ilianza kujengwa 2026-09-16 lakini iliondolewa kwa ombi la mtumiaji** (issue ya TRA integration — mtumiaji ataamua baadaye jinsi ya kuendelea; hakuna kilichobaki kwenye code — ilikuwa haijasukumwa/kutumwa popote, ilirudishwa salama). Receipt template designer (JSON, Hatua 6) bado haijaanza — hii "risiti ya kitaalamu" hapo juu ni toleo rahisi la Hatua 4, si mfumo kamili wa templates.

## Hatua 5 — P1: ✅ IMEKAMILIKA (kwa vitendo)
- ✅ **Wasambazaji — msingi wa kwanza (2026-09-16)**: `suppliers.php` (list/get/add/update/delete, self-creating table, sawa na customers.php), `product_batches.supplier_id` (nullable, self-added) kuunganisha batch za stock na msambazaji. App: `Supplier` model, `crm_api.dart` supplier methods (**online-only kwa makusudi** — si sehemu ya njia ya offline-write ya wakati wa mauzo kama customers), `suppliers_screen.dart` (list/search/CRUD + `pickMode` inayotumika na `add_batch_sheet.dart` badala ya free-text field ya zamani). Tab mpya "Wasambazaji" kwenye Manage (Manager/Stockist wana `suppliers.*`, Accountant `suppliers.view` pekee — bootstrap.php). `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live, push (commit 5bda08c).
- ✅ **Manunuzi + deni kwa msambazaji (2026-09-16)**: `purchases.php` (list/get/create/record_payment, self-creating, client_op_id idempotent kama sale_returns.php). "Create" moja inajumuisha kupokea+kuandikisha (si PO tofauti na GRN — sawa na jinsi duka ndogo halisi inavyofanya kazi). Kila item inaongeza stock kupitia `product_batch_add()` ile ile ya `add_product_batch.php` (FIFO, kiungo na msambazaji) + UPDATE tbl_product.stock — njia MOJA ya kuongeza stock, si mbili tofauti. `paid_amount < subtotal` inaacha deni (payment_status unpaid/partial/paid), inalipwa taratibu kupitia `record_payment`. `suppliers.php` sasa inaonyesha `total_owed` kwa kila msambazaji. App: `PurchasesScreen` (list+filta, fomu ya kuongeza ununuzi yenye utafutaji wa bidhaa wa ndani — SI CartProvider ya mauzo, kwa makusudi kuepuka mchanganyiko), tab "Manunuzi" kwenye Manage. **Iligundua na kurekebisha mgongano wa icon** — `add_shopping_cart_rounded` ilikuwa tayari inatumika na kitufe cha POS, ikasababisha test ya screenshot kushindwa (ilibonyeza icon isiyo sahihi) — imebadilishwa kuwa `move_to_inbox_outlined`. `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live, push (commit 8211e8c).
- ✅ **Mtiririko wa Pesa kwa aina ya malipo (2026-09-17)**: mtumiaji aliomba "endelea" na accounts/PO/stock transfer baada ya COGS batch-fix. Nimechagua cash flow kwanza (badala ya stock transfer, ambayo ina swali la usanifu bado wazi kuhusu bidhaa za matawi tofauti). `sale_payments` sasa ni orodha KAMILI ya pesa-ndani kwa kila mauzo yaliyolipwa (si split payments pekee tena) — `helpers/sale_payments.php` (`sale_payment_log()`) inaitwa na `create_sale.php` na `sale_action.php` (record_payment/collect_cash/confirm_bank_transfer). `tbl_expenses` ina column mpya `payment_method` (self-healing, default 'cash'). Endpoint mpya `cashflow.php`: pesa-ndani (sale_payments) vs pesa-nje (purchase_payments + expenses) vs net, kwa kila method. App: `getCashFlow()` (online+offline), tab ya 4 "Mtiririko wa Pesa" kwenye Reports. **Data ya zamani (kabla ya deploy hii) haina sale_payments rows** isipokuwa zile zilizokuwa split payments — pesa-ndani ya vipindi vya nyuma itaonekana chini ya ukweli; mauzo mapya kuanzia sasa ni kamili. `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live (hakuna 500), push (commit f2cf823).
- ✅ **Supplier statement kamili + invoice rasmi (2026-09-17)**: mtumiaji aliomba "malizia iyo ya tano". `suppliers.php?action=get` sasa inarudisha `statement`: kila ununuzi = DEBIT (subtotal_amount), kila malipo (purchase_payments, ikijumuisha "malipo ya awali") = CREDIT, vyote vikiwa vimepangwa kwa tarehe na salio linalobadilika (running balance) — server-side, si app inayochanganya vyanzo viwili. App: `_SupplierDetailSheet` mpya (kubonyeza msambazaji sasa kunafungua taarifa, si fomu ya edit moja kwa moja) yenye "Chapisha Taarifa" (PDF A4, jedwali). `purchases_screen.dart` `_PurchaseDetailSheet` ina kitufe cha kuchapisha invoice rasmi ya PDF. `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live, push (commit b81ce67).
- ✅ **PO rasmi — kuagiza kama hatua tofauti na kupokea (2026-09-17)**: `purchase_orders.php` mpya (jedwali `purchase_orders`/`purchase_order_items`, self-creating): create huunda 'draft' (haigusi stock wala deni), 'send' inaashiria imetumwa kwa msambazaji, 'cancel' inafuta (isipokuwa iliyopokewa), 'receive' ndipo inapotengeneza rekodi ya kawaida ya `purchases` (stock + batch + deni — logic ile ile ya purchases.php 'create', ikiruhusu override ya kiasi/bei wakati wa kupokea kama kilichofika ni tofauti na kilichoagizwa). App: `PurchaseOrdersScreen` (list+filta status, fomu ya kuagiza, detail na vitufe Tuma/Futa/Pokea), tab "Maagizo" kwenye Manage (icon `request_quote_outlined` — imethibitishwa haina mgongano na icon nyingine yoyote kwenye app kabla ya kutumika, somo kutoka kwenye mgongano wa awali wa Manunuzi). `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live, push (commit b81ce67).
- ✅ **Kila tawi lina stock yake tofauti + Uhamisho wa stock (2026-09-17)**: kabla ya kujenga stock transfer, niligundua `get_products.php` ilikuwa ikionyesha bidhaa ZOTE za biashara bila kuchuja kwa tawi (`branch_id` ilikuwepo lakini haikutumika), na `add_product.php`'s duplicate-check ilikuwa ikiangalia biashara nzima si tawi moja — kwa pamoja hii ilimaanisha matawi yote ya biashara moja yalishirikiana hifadhi MOJA ya stock (si kila tawi lake). **Niliuliza mtumiaji** kabla ya kubadilisha (hatari kwa data ya live) — alichagua "kila tawi liwe na stock yake tofauti". Nilithibitisha kwa script ya uchunguzi ya muda (ilifutwa baada) kuwa business_id=13 ina TAWI MOJA pekee (mahenge, id=5) — hivyo marekebisho ni salama 100% kwa data ya sasa (idadi ya bidhaa ilibaki 491 kabla na baada). Marekebisho: `get_products.php` sasa inachuja kwa `branch_id` ikiwa imetumwa (app ilikuwa tayari ikiituma — client haikuhitaji kubadilika); `add_product.php` duplicate-check sasa ina branch_id. Endpoint mpya `stock_transfers.php` (jedwali `stock_transfers`/`stock_transfer_items`, self-creating): inapunguza stock ya tawi la chanzo (FEFO-weighted cost, logic ile ile ya create_sale.php), kisha inatafuta/kuunda bidhaa ya tawi lengwa kwa product_code/barcode. App: `StockTransfersScreen`, tab "Uhamisho" kwenye Manage — **inaonekana tu kwa biashara zenye matawi 2+** (business_id=13 ina tawi moja, hivyo tab haionekani kwao mpaka waongeze tawi la pili — hakuna athari kwa sasa). `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live (idadi ya bidhaa 491=491 kabla/baada), push (commit 5691fb2).
- ❌ **Kimakusudi HAIJAJUMUISHWA** (mradi mkubwa zaidi, ijayo): invoice ya msambazaji kama hati rasmi ya kudumu (ipo tu kama print-on-demand kwa sasa, si document iliyohifadhiwa).

## Hatua 6 — P2: 🟡 IMEANZA
- ✅ **Ripoti ya bidhaa zinazokaa/kupoteza thamani (2026-09-17)**: mtumiaji alichagua hii kama kianzio cha Hatua 6 (kati ya chaguo 3: hii, hati za biashara, uhasibu). Endpoint mpya `slow_stock.php`: bidhaa zenye stock>0 zinaainishwa 'dead' (hazijauzwa kabisa ndani ya siku 60 zilizopita, default) au 'slow' (siku za kumaliza stock kwa kasi ya sasa > siku 90, default) — zinazouzwa haraka HAZIONEKANI kwenye ripoti hii kwa makusudi. Imepangwa kwa "tied_capital" (stock × purchase_price) ili pesa kubwa zaidi iliyofungwa ionekane kwanza; pia inaonyesha muda wa mwisho wa kuisha (nearest_expiry kutoka product_batches) ikiwepo. Tab ya 5 "Bidhaa Zinazokaa" kwenye Reports. Imethibitishwa live dhidi ya business_id=13 (data halisi — bidhaa kadhaa za TZS 3,000-4,500 kila moja zimekaa bila kuuzwa kwa zaidi ya siku 138). `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live, push (commit 3ac812b).
- ✅ **Nukuu ya Bei + Hati ya Usafirishaji (2026-09-17)**: mtumiaji alichagua "hati za biashara" kama kipaumbele kinachofuata. `quotations.php` mpya (jedwali `quotations`/`quotation_items`, self-creating): nukuu ya bei kwa mteja KABLA ya mauzo — HAIGUSI stock wala fedha kabisa (ni bei iliyopendekezwa tu, si document ya kihasibu). Inaruhusu bidhaa za katalogi (product_id) AU safu za kawaida za maandishi (huduma n.k. bila product_id). Lifecycle: create→'sent', kisha 'accept'/'cancel'/'expire' (informational tu — mauzo halisi bado yanafanywa kwa kawaida kupitia POS, hakuna "convert to sale" ya moja kwa moja kwa makusudi, ingehitaji kugusa CartProvider kwa hatari isiyo ya lazima). App: `QuotationsScreen` (tab "Nukuu" kwenye Manage) yenye customer picker (`CustomersScreen(pickMode:true)`, tayari ilikuwepo), product search, PDF export. Delivery note: kitufe kipya "Chapisha Hati ya Usafirishaji" kwenye `transaction_detail_sheet.dart` (PDF ya idadi tu, bila bei, yenye mistari ya saini — HAKUNA backend mpya, inatumia data ya sale iliyoshapakiwa). Nilijaribu `create` moja kwa moja dhidi ya live (client_op_id ya alama) kuthibitisha njia kamili inafanya kazi, kisha nikafuta rekodi hiyo ya jaribio mara moja (script ya muda, ilifutwa baada) ili isibaki kwenye data halisi ya mtumiaji. `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live, push (commit d1d45a9).
- ✅ **P&L PDF export + filter ya kategoria (2026-09-17)**: kitufe cha PDF (hati rasmi ya A4 ya kuchapisha/kutuma — benki, mwenye jengo n.k.) kando ya kitufe cha Excel kilichopo kwenye P&L tab, kikitumia pw.Document/Printing pattern ile ile ya invoice/quotation. "Bidhaa zinazouzwa zaidi" (Sales tab) sasa ina chips za kuchuja kwa kategoria — client-side tu (data ilishapakuliwa), hakuna badiliko la backend. `flutter analyze` 0, `flutter test` 27/27, push (commit 266022d).
- ✅ **Receipt template designer (2026-09-17)**: mipangilio ya biashara (`business_select_screen.dart`'s edit sheet, kando ya receipt_header/footer zilizokuwepo) sasa ina toggles 6: logo, QR code, jina la muuzaji, jina/namba ya mteja, anuani ya biashara (mpya), namba ya simu ya biashara (mpya). Zinahifadhiwa kama JSON kwenye `businesses.receipt_template` (column mpya, self-healing ALTER — sawa na migration nyingine zote za mradi huu). `ReceiptTemplate` model mpya (`lib/models/business.dart`) na default zinazolingana KABISA na tabia ya sasa (logo/QR/cashier/customer = onyesha, kama ilivyokuwa siku zote) — **hakuna risiti inayobadilika kwa mtumiaji yeyote mpaka waingie kwenye mipangilio na kubadilisha wenyewe**. `pos_screen.dart`'s `_printReceipt` (PDF ya risiti halisi ya mauzo) sasa inasoma template hii kuamua ni sehemu zipi za kuchapisha. `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live (self-healing column ilithibitishwa kufanya kazi bila 500), push (commit a18ff63).
- ✅ **Muhtasari wa Akaunti — "uhasibu rahisi" (2026-09-17)**: mtumiaji alipewa chaguo 3 (muhtasari rahisi / double-entry kamili / ruka) — alichagua muhtasari rahisi kwa sababu double-entry ingegusa KILA operesheni ya fedha (mauzo/manunuzi/matumizi/malipo) kwa hatari kubwa, wakati app tayari ina Cash Flow/P&L/statements zinazofunika mengi. `account_summary.php` mpya inahesabu Mali (fedha kwa kila njia kutoka sale_payments/purchase_payments/tbl_expenses — SALIO all-time hadi tarehe, si mtiririko wa kipindi kama cashflow.php; deni la wateja kutoka tbl_sales zisizolipwa; thamani ya stock kutoka stock×purchase_price), Madeni (deni kwa wasambazaji kutoka purchases zisizolipwa), na Thamani Halisi (Mali − Madeni) — HAKUNA jedwali jipya la journal/ledger, yote yamehesabiwa kutoka data iliyopo. Tab ya 6 kwenye Reports, na onyo dhahiri kwenye UI kwamba hii ni "makadirio", si uhasibu rasmi wa double-entry. Imethibitishwa live dhidi ya business_id=13: thamani ya stock TZS 166,109, faida ya uendeshaji (tangu mwanzo) TZS 1,993 — fedha taslimu inaonyesha 0 kwa usahihi kwa sababu sale_payments (chanzo cha fedha) ilianza kuandikwa KAMILI leo hii (mauzo ya zamani hayana rekodi — kikwazo kilichoelezwa tayari kwenye cashflow.php). `flutter analyze` 0, `flutter test` 27/27, push (commit ef827e4).
- ❌ **Bado** (Hatua 6): design system ya Flutter. Uhasibu rasmi wa double-entry umeruhusiwa kuachwa kimakusudi kwa ombi la mtumiaji (angalia hapo juu).

## Ripoti/P&L (reports.php) — usahihi
- ✅ **Marekebisho (2026-09-17)**: mtumiaji aliuliza "report ya P&L ipo safi?" — nikagundua returns hazikupunguza COGS kwenye P&L (revenue ilipungua sahihi lakini cost hapana, faida ikaonekana chini kuliko halisi). Imerekebishwa: `reports.php` sasa inahesabu `returned_cogs_credit` na kuitoa kwenye COGS, kwa single-range na monthly series zote mbili, ikizingatia tarehe ya MAUZO ASILI (si ya return) — sambamba na jinsi revenue tayari inavyofanya kazi. Imethibitishwa live dhidi ya business_id=13 (mauzo 29 halisi) — hakuna regression. Hakuna returns halisi bado kwenye database kujaribu njia mpya yenyewe, lakini logic ya query imehakikiwa moja kwa moja. Push (commit 9fc2417).
- ✅ **Kikwazo cha COGS batch-level kimetatuliwa (2026-09-17)**: `create_sale.php` sasa inahesabu `cost_price` ya `tbl_sale_items` kama WASTANI ULIOPIMWA (weighted average) wa batch(es) HALISI ambazo FEFO imezitumia kwa kila line-item (`sum(take × batch.buy_price) / total qty`), si `tbl_product.purchase_price` ya wakati huo. Deduct-stock step ilihamishwa kutokea KABLA ya sale_items insert (haikuwa na sababu ya kutokea baada — hazitegemeani). Ikiwa batches hazitoshi kufunika kiasi (mfano stock ya kabla ya batch tracking), sehemu isiyofunikwa inachanganywa na purchase_price ya sasa (hakuna kipimo kinachobaki bila gharama). **Mauzo ya ZAMANI hayaguswi** (cost_price ni "picha ya wakati huo", si rejea inayobadilika) — ni mauzo MAPYA tu kuanzia sasa yatakuwa na gharama sahihi zaidi. `php -l` clean, curl smoke-test (insufficient-stock path) live dhidi ya business_id=13 — hakuna 500. **Sijatengeneza mauzo halisi ya kujaribu kwenye data ya live** ili nisichafue historia yao halisi ya mauzo — nilitegemea ukaguzi wa kina wa mkono wa logic badala yake. Push (commit 476eb31).

## FTP / Deploy
- Credentials: `%USERPROFILE%\.duka_ftp.env` (FTP_HOST=ftp.dadcas.com, API dir `/pos/api`, web dir `/pos/app`) — **nje ya repo, usi-print**.
- Tools: `tools/deploy.py` (check | pull-api | patch-api | push-api | build-web | push-web | web | api), `tools/sync_web.py` (resumable web deploy).
- 2026-09-15: FTP imeunganishwa ✅ — local mirror PHP 53/53 zipo server (0 missing). Server ina pia `.bak_before_sync` backups.
- Windows note: tumia `PYTHONIOENCODING=utf-8` kabla ya python (iziepuke UnicodeEncodeError ya ✓).

# Kazi inayoendelea / zinazofuata (kwa mpangilio)

1. **Hatua 2 — kilichobaki**: (a)-(d) zote ✅ zimekamilika (customer_id, backfill, split payment+change, returns/refunds+credit notes). Kimebaki tu (e) Checkout widget moja (desktop `_CartPanel` + simu `_CartSheet` — uamuzi wa makusudi kuahirisha, si muhimu). **Hatua 2 kwa vitendo IMEKAMILIKA.**
2. **Hatua 3**: ~~batches mbili → moja~~ ✅, ~~FEFO~~ ✅, ~~approval ya adjustments (Manager PIN)~~ ✅ (zote 2026-09-16). **Hatua 3 kwa vitendo IMEKAMILIKA.** Kimebaki tu vitu visivyo P0: StockService moja (usanifu, si bug), opening backfill kwa bidhaa za zamani (haijathibitishwa).
3. **Usalama wa ziada (Hatua 1 residuals)**: badilisha DB/FTP passwords (za sasa zimeonekana kwenye chat — dharura); ~~backup ya usiku~~ ✅ script tayari (`db_backup.php`) — **inasubiri tu mtumiaji aweke Cron Job kwenye cPanel** (tazama Hatua 1 hapo juu kwa command); offsite copy bado haijapangwa.
4. Kabla ya 2026-10-31: hakikisha app zote zimetoka na token auth, kisha weka `AUTH_ALLOW_LEGACY = false`.
5. **Hatua 4 (P1) — IMEKAMILIKA**: ~~discount + manager PIN~~ ✅, ~~hold/resume sale~~ ✅, ~~risiti ya kitaalamu (logo/cashier/QR/paper size)~~ ✅ (2026-09-16), ~~register/shift~~ ✅ (2026-09-17). (VAT ilianza, iliondolewa kwa ombi la mtumiaji — angalia Log.)
6. **Hatua 5 (P1) — IMEKAMILIKA**: ~~wasambazaji — msingi~~ ✅, ~~manunuzi + deni kwa msambazaji~~ ✅ (2026-09-16), ~~mtiririko wa pesa~~ ✅, ~~supplier statement kamili~~ ✅, ~~invoice rasmi~~ ✅, ~~PO rasmi~~ ✅, ~~kila tawi stock yake + stock transfer~~ ✅, ~~reconciliation~~ ✅ (kupitia register/shift ya Hatua 4) (zote 2026-09-17).
7. **Hatua 6 (P2) — karibu kukamilika kwa uchaguzi wa mtumiaji**: ~~ripoti ya bidhaa zinazokaa/kupoteza thamani~~ ✅, ~~nukuu ya bei + hati ya usafirishaji~~ ✅, ~~ripoti filters+PDF export~~ ✅, ~~receipt template designer~~ ✅, ~~muhtasari wa akaunti (uhasibu rahisi, si double-entry)~~ ✅ (zote 2026-09-17). Kimebaki: design system.

# Maamuzi muhimu (yaliyokubaliwa)

- Architecture: "usibomoe" — PHP backend inabaki; tabaka moja la kati (bootstrap.php: auth + permissions + audit + validation); endpoints zinaita helpers.
- Offline-first inabaki: kila feature mpya ya kuandika ipite `OfflineApiService` (online → cache; offline → queue, `client_op_id` idempotency).
- Kila migration inaongeza tu (ADD COLUMN/CREATE TABLE), inabeba data ya zamani, ina script ya kurudi nyuma, inajaribiwa kwenye copy ya DB kwanza.
- Apps ndogo `kukupaja_*` / `kukusoko_*` haziguswi bila ruhusa.

# Log (mfululizo wa kazi)

- 2026-09-17 (11): **Hatua 6: Muhtasari wa Akaunti (uhasibu rahisi)** — mtumiaji alipewa chaguo 3 (rahisi/double-entry kamili/ruka) kwa sababu double-entry ni kazi kubwa yenye hatari (ingegusa kila operesheni ya fedha) na thamani ndogo ya ziada (app tayari ina Cash Flow/P&L/statements). Alichagua rahisi. `account_summary.php` mpya: Mali (fedha kwa njia, deni la wateja, thamani ya stock), Madeni (deni kwa wasambazaji), Thamani Halisi — yote kutoka data iliyopo, hakuna jedwali jipya la ledger. Tab ya 6 kwenye Reports. Imethibitishwa live — fedha taslimu ni 0 kwa usahihi (mauzo ya zamani hayana sale_payments, kikwazo kilichoelezwa awali). `flutter analyze` 0, `flutter test` 27/27, push (commit ef827e4).
- 2026-09-17 (10): **Hatua 6: Receipt template designer** — mtumiaji alichagua hii kati ya chaguo 2 (hii au uhasibu) baada ya P&L PDF export. Toggles 6 (logo/QR/cashier/customer/address/phone) kwenye mipangilio ya biashara, zinahifadhiwa kama JSON kwenye `businesses.receipt_template` (self-healing column). Default zinalingana kabisa na tabia ya sasa — hakuna risiti inayobadilika bila mtumiaji kufungua mipangilio mwenyewe. `pos_screen.dart`'s receipt PDF sasa inasoma template hii. `flutter analyze` 0, `flutter test` 27/27, deployed live (column migration ilithibitishwa bila 500), push (commit a18ff63).
- 2026-09-17 (9): **Hatua 6: P&L PDF export + filter ya kategoria** — mtumiaji aliuliza "nini kimebakia" baada ya register/shift, kisha "endelea". Nimeongeza kitufe cha PDF (hati rasmi ya kuchapisha/kutuma) kando ya Excel iliyopo kwenye P&L, na chips za kuchuja "Bidhaa zinazouzwa zaidi" kwa kategoria (client-side). Hakuna badiliko la backend. `flutter analyze` 0, `flutter test` 27/27, push (commit 266022d).
- 2026-09-17 (8): **Register/Shift + Reconciliation — Hatua 4 NA Hatua 5 zote zinakamilika** — mtumiaji alisema "endelea" baada ya kupendekeza register/shift kama hatua yenye thamani kubwa zaidi (inafungua vitu viwili kwa pamoja). `shifts.php`: fungua zamu na fedha ya kuanzia, funga ukihesabu fedha taslimu — mfumo unahesabu inayotarajiwa dhidi ya uliyohesabu (variance), ikitumia sale_payments/tbl_expenses zilizopo tayari (hakuna jedwali jipya la miamala). Hii YENYEWE ndiyo reconciliation ya Hatua 5. Wakati wa smoke-test live niligundua bug halisi: `??` haiangalii "0" kama thamani isiyokuwepo, hivyo user_id ya mtumiaji aliyeko ilikuwa ikipuuzwa wakati wa dirisha la legacy — nimeirekebisha. Nilijaribu mzunguko kamili fungua→funga dhidi ya live (mtumiaji wa kubuni), nikathibitisha hesabu ni sahihi, kisha nikafuta data ya jaribio. Kitufe kipya cha "Zamu" kwenye POS (desktop+simu), sikuongeza kizuizi cha kulazimisha zamu ifunguliwe kabla ya kuuza (hiari kwa makusudi, kuepuka kuvuruga watumiaji waliopo). `flutter analyze` 0, `flutter test` 27/27, push (commit 99da20f).
- 2026-09-17 (7): **Hatua 6: Nukuu ya Bei + Hati ya Usafirishaji** — mtumiaji alichagua "hati za biashara" kama kipaumbele kinachofuata kati ya chaguo 3 (hii, register/shift, uhasibu). `quotations.php` mpya — bei kwa mteja kabla ya mauzo, haigusi stock/fedha, inaruhusu bidhaa za katalogi au safu za maandishi ya kawaida (huduma). Delivery note ni kitufe kipya tu kwenye sale detail (hakuna backend), kinatumia data ya sale iliyoshapakiwa kutengeneza PDF ya idadi + saini. Nilijaribu create halisi dhidi ya live kuthibitisha kabisa njia mpya inafanya kazi, kisha nikafuta data hiyo ya jaribio mara moja. `flutter analyze` 0, `flutter test` 27/27, push (commit d1d45a9).
- 2026-09-17 (6): **Hatua 6: Ripoti ya bidhaa zinazokaa/kupoteza thamani** — baada ya Hatua 5 kukamilika karibu kabisa, nilimpa mtumiaji chaguo 3 za kianzio cha Hatua 6 (ripoti hii, hati za biashara, uhasibu) — alichagua hii kwa sababu ni ndogo na ina thamani ya haraka. `slow_stock.php` mpya inaainisha bidhaa 'dead' (hazijauzwa) au 'slow' (zinauzwa pole sana), imepangwa kwa pesa iliyofungwa (tied_capital), tab ya 5 kwenye Reports. Imethibitishwa live dhidi ya data halisi ya business_id=13 — bidhaa kadhaa za maelfu ya shilingi kila moja zimekaa bila kuuzwa zaidi ya siku 138. `flutter analyze` 0, `flutter test` 27/27, push (commit 3ac812b).
- 2026-09-17 (5): **Hatua 5: Kila tawi stock yake + Uhamisho wa stock kati ya matawi** — mtumiaji alisema "endelea, malizia iyo ya tano". Kabla ya kujenga stock transfer niligundua get_products.php haikuchuja kwa branch_id (matawi yote yalishirikiana hifadhi moja) na add_product.php's duplicate-check haikuwa branch-aware. **Niliuliza mtumiaji kabla ya kubadilisha** (hatari kwa data ya live) kwa AskUserQuestion — alichagua "kila tawi liwe na stock yake tofauti". Nilithibitisha kwa script ya muda kuwa business_id=13 ina tawi moja pekee (salama 100% kubadilisha). Marekebisho + stock_transfers.php mpya (FEFO-weighted transfer, auto-create bidhaa ya tawi lengwa). Idadi ya bidhaa ilibaki 491=491 kabla/baada ya marekebisho — hakuna regression. `flutter analyze` 0, `flutter test` 27/27, push (commit 5691fb2).
- 2026-09-17 (4): **Hatua 5: Supplier statement kamili + Invoice rasmi + PO rasmi** — sehemu tatu za mwisho za Hatua 5 kwa ombi la mtumiaji. `suppliers.php` sasa inarudisha statement (debit=manunuzi, credit=malipo, salio linalobadilika) na PDF export; purchases zina invoice ya PDF; `purchase_orders.php` mpya inatenganisha "kuagiza" (draft/sent, haigusi stock) na "kupokea" (inatengeneza purchase ya kawaida). `flutter analyze` 0, `flutter test` 27/27, push (commit b81ce67).
- 2026-09-17 (3): **Hatua 5: Mtiririko wa Pesa (Cash Flow) kwa aina ya malipo** — mtumiaji aliomba "endelea" na accounts/PO/stock transfer baada ya COGS batch-fix. Nimechagua cash flow kwanza kama hatua salama na yenye thamani pana zaidi ya stock transfer (ambayo bado ina swali la usanifu wazi kuhusu tbl_product.branch_id moja kwa kila bidhaa). `sale_payments` sasa ni orodha kamili ya pesa-ndani kwa kila mauzo (si split payments pekee), `tbl_expenses.payment_method` mpya, endpoint mpya `cashflow.php` (in/out/net kwa method), tab ya 4 kwenye Reports. `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live, push (commit f2cf823). Angalia `server_patch/migration_cashflow.sql`.
- 2026-09-17 (2): **COGS batch-level costing (FEFO-weighted)** — mtumiaji aliomba nishughulikie kikwazo kilichoripotiwa awali. `create_sale.php` sasa inahesabu `cost_price` kama wastani uliopimwa wa batch(es) halisi zilizotumika na FEFO, si `tbl_product.purchase_price` ya sasa. Mauzo mapya tu yanaathiriwa (ya zamani ni snapshot, hayabadiliki). `php -l` clean, curl smoke-test live (hakuna 500) — sijatengeneza mauzo ya kujaribu kwenye data halisi ya live kuepuka kuchafua historia yao. Push (commit 476eb31).
- 2026-09-17 (1): **Marekebisho ya P&L: returns hazikupunguza COGS** — mtumiaji aliuliza kama P&L ni safi; nikafanya ukaguzi kamili wa `reports.php` dhidi ya features zote zilizojengwa leo (returns, discount, split payment, purchases) na kupata bug halisi: returns zilipunguza revenue lakini si COGS, hivyo faida ilionekana chini kuliko halisi. Imerekebishwa + imethibitishwa live. Pia nimeandika (bila kutatua — si dharura) kikwazo cha awali cha usahihi wa COGS kisichohusiana na FEFO batch-level costing. Push (commit 9fc2417).
- 2026-09-16 (13): **Hatua 5: Manunuzi (purchases) + deni kwa msambazaji** — `purchases.php` (list/get/create/record_payment), stock inaongezwa kupitia njia ileile ya `product_batch_add()` (si njia mpya sambamba), deni linalipwa taratibu. `PurchasesScreen` + tab "Manunuzi". Niligundua mgongano wa icon (`add_shopping_cart_rounded` ilikuwa tayari inatumika na kitufe cha POS) uliovunja screenshot test — nilifuatilia mpaka chanzo (finder ilibonyeza tab isiyo sahihi ya Manage) na kuubadilisha. `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live, push (commit 8211e8c).
- 2026-09-16 (12): **Hatua 5: Wasambazaji (suppliers) — msingi wa kwanza** — mtumiaji aliomba "anza na hatua ya 5-7" baada ya Hatua 4 kuendelea vizuri. Nimechagua kuanza na "suppliers" pekee (bila PO/GRN/invoice/payment — hayo ni mradi mkubwa zaidi wa baadaye) kwa sababu ndiyo msingi unaohitajika kabla ya kitu kingine chochote kwenye mnyororo huo. `suppliers.php`, `product_batches.supplier_id`, `suppliers_screen.dart` + supplier picker kwenye `add_batch_sheet.dart`, tab mpya kwenye Manage. `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live, push (commit 5bda08c).
- 2026-09-16 (11): **Risiti ya kitaalamu zaidi** — logo, jina la cashier, QR code, chaguo la ukubwa wa karatasi (58mm/80mm/A4, kumbukumbu per-device). `flutter analyze` 0, `flutter test` 27/27, push (commit 344803e).
- 2026-09-16 (10): **VAT ilijaribiwa kisha ikaondolewa** — nilijenga VAT settings (toggle+rate+VRN kwenye business settings) + mchanganuo wa VAT kwenye risiti (bila TRA/EFD integration, kama mtumiaji alivyoomba). Mtumiaji aliomba niiondoe kabla ya kuisukuma/kuituma popote ("toa issue ya vat kwasasa kwanza") — imeondolewa kikamilifu (`git checkout` kwa faili za Flutter zilizokuwa hazijahifadhiwa, faili mpya za PHP zimefutwa, hakuna kilichofika kwenye git wala server). Somo: VAT bado ni wazo halali la baadaye, lakini mtumiaji anataka kuamua muda/mkakati wake mwenyewe (labda anasubiri uamuzi kuhusu TRA/EFD kwanza).
- 2026-09-16 (9): **Hold/Resume Sale** — `held_sales_provider.dart` (local-only, kv_cache), `held_sales_sheet.dart` UI, kitufe "Weka Kando" + badge kwenye POS (desktop+simu). `CartItem.toJson/fromJson` ziliongezwa. Wakati wa kujaribu niligundua overflow ndogo ya layout kwenye desktop panel (320px width) iliyosababishwa na vitufe vipya, na `HeldSalesProvider` ilikosekana kwenye screenshot test's MultiProvider (ilisababisha ProviderNotFoundException iliyoonekana kama overflow kubwa) — zote mbili zimerekebishwa. `flutter analyze` 0, `flutter test` 27/27. Hakuna backend/DB. Push (commit 3065d75).
- 2026-09-16 (8): **Hatua 4: Punguzo la jumla + idhini ya meneja** — `discount_field.dart` (UI, imezimwa default), `create_sale.php` inakubali `overall_discount` (>10% ya subtotal inahitaji manager_pin, kutumia helpers/manager_pin.php ile ile ya stock write-offs), split payment + risiti sasa zinatumia grand total (baada ya punguzo). `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live, push (commit 65d1a93). Hatua 4 (P1) imeanza kabla ya wakati wake kwa sababu ilikuwa rahisi kutumia miundombinu ya PIN iliyojengwa kwa Hatua 3.
- 2026-09-16 (7): **Manager PIN approval kwa stock write-offs** — `helpers/manager_pin.php`, `manager_pin.php` endpoint, `manager_pin_dialog.dart` UI, kitufe cha kuweka PIN kwenye Profile, kimeunganishwa kwenye `StockAdjustSheet`. `flutter analyze` 0, `flutter test` 27/27, deployed + smoke-tested live (pin_hash column self-created, no 500s), push (commit a447ab0). **Hatua 3 (Stock ledger) sasa IMEKAMILIKA kwa vitendo** — P0 zote za Hatua 1, 2, na 3 kutoka audit ya 2026-09-14 zimekamilika.
- 2026-09-16 (6): **Hatua 3: batch tables unified + FEFO** — `helpers/batches.php` (`product_batch_add()`), `add_product.php`/`import_products.php` sasa zinaandika `product_batches` (si `tbl_product_stock_batch` pekee), `create_sale.php`/`get_product_batches.php` zina FEFO order. `add_stock.php` imethibitishwa dead code (haijaguswa). Backend-only, `php -l` zote clean, deployed + smoke-tested live (0 files no 500), push (commit 2a9007b).
- 2026-09-16 (5): **Credit notes** — `_CreditNoteSheet` kwenye `sale_return_sheet.dart` inachapisha hati rasmi ya marejesho baada ya return, kwa PDF (pattern ileile ya risiti). Hakuna backend/DB mabadiliko. `flutter analyze` 0, `flutter test` 27/27, push (commit c543a40). **Hatua 2 (Wateja/Malipo/Marejesho) sasa IMEKAMILIKA kwa vitendo** — kimebaki tu Checkout widget moja, kilichoahirishwa kwa makusudi.
- 2026-09-16 (4): **Backfill ya wateja imekaguliwa live** — haihitajiki zaidi (40 sales jumla, 0 zina phone-bila-customer_id; 35 hazina phone kabisa hivyo haziwezi kuunganishwa). Kipengele kilichodhaniwa "pending" tangu 2026-09-15 kimebainika kimekamilika tayari kupitia backfill ya awali + trigger `trg_sales_bi`. Hatua 2 sasa iko karibu kukamilika kabisa (kimebakia credit notes rasmi + checkout widget moja, zote mbili zimeahirishwa kwa sababu tofauti).
- 2026-09-16 (3): **Split payment (Cash+M-Pesa+Bank) + chenji** — `sale_payments` table (self-creating), `create_sale.php` inapokea `payments[]` na kuhesabu `change_amount`, `get_sale_payments.php` inarudisha `breakdown`, UI mpya `split_payment_field.dart` (toggle, default imezimwa), chips kwenye `transaction_detail_sheet.dart`, offline queue inapitisha `payments` kupitia sync. Wakati wa kazi hii niligundua **`pay_loan.php` ni dead code yenye column mismatch** dhidi ya live schema (tazama Hatua 2 hapo juu) — sijaigusa, nje ya scope. `flutter analyze` 0, `flutter test` 27/27, deploy live + push (commit fa58a91) kwenye PR #1.
- 2026-09-16 (2): **DB backup ya usiku** — `db_backup.php` (pure-PHP mysqldump-equivalent) + `backup_key.php` (secret, sio kwenye git) vime-deploy kwenye server; imejaribiwa live (jedwali 53, safu 2314, faili ~47KB, 403 kwa yeyote asiye na key, 403 kwa directory listing). Haijaongezwa kwenye git (iko `server_mirror/` — gitignored kwa makusudi, kama `db.php`). Inasubiri mtumiaji aweke Cron Job kwenye cPanel.
- 2026-09-16 (1): `customer_id` imeunganishwa kwenye create_sale (offline+online), na feature ya **Returns/Refunds** kamili (`sale_returns.php` + `sale_return_sheet.dart` + `canReturnSales` permission) imejengwa, ime-deploy live, `flutter analyze` 0 issues, `flutter test` 27/27 ✅, imesukumwa (push) kwenye branch `feature/hatua-1-3-security-crm-stock-ledger` (commit 3f9861a) juu ya PR #1 iliyopo.
- 2026-09-15: FTP imeunganishwa (deploy.py check ✅, 0 files missing kwenye server); uthibitisho wa code: Hatua 1 ✅ ime-deploy (bootstrap.php/tokens/audit/throttle), Hatua 2 nusura (customers ✅, malipo historia ✅; split/returns/customer_id-on-sale ❌), Hatua 3 ilianza (stock_movements ✅). Memory + skill `project-memory` zimetengenezwa.
- 2026-09-14: Audit kamili (features 140) + mpango wa phases 1–7; kazi za server endpoints + deploy tools + Jikoni intake ya Excel. (Maelezo ya sessions za kabla ya 2026-09-15 hayapatikani tena; file hii ndiyo kumbukumbu rasmi kuanzia sasa.)

---

# Sheria za kujibu (kwa Buffy)

1. Kiswahili rahisi, mfupi.
2. Usibadilishe apps za `kukupaja_*` / `kukusoko_*` bila ruhusa.
3. Kabla ya deploy (sync_web.py / deploy.py push-api / push-web) — uliza ruhusa.
4. Credentials haziprintwi kamwe; ziko `%USERPROFILE%\.duka_ftp.env`.
5. Typecheck: `flutter analyze` kabla ya kumalizia kazi kubwa.
6. Windows bash: `PYTHONIOENCODING=utf-8` kwa python scripts.
